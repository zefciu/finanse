My private project to maintain domestical spendings

Rather not for end-users yet. Will contain some console scripts that talk to 
database via sqlalchemy.

Ok. Now it uses cherryPy and ExtJS (not included in repo)
